User API for interacting with user table inside planthealthcare DB.
CRUD operations exposed.

GET all         /users/
GET one         /users/<id>
POST one        /users/
PUT one         /users/<id>
DELETE one      /users/<id>

Building docker image:
docker build -t [tag-name] https://github.com/markocrnic/user-API.git

Running docker image:
docker run -d -p 5000:5000 [user-API-tag-name]
